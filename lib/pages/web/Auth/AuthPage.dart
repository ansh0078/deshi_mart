import 'package:flutter/material.dart';

class WebAuthPage extends StatelessWidget {
  const WebAuthPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Web Auth page")],
      ),
    );
  }
}